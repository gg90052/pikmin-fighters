{
  "name": "pikmin-fighters",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^12.11.0",
    "vue": "^3.5.30"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "@vitejs/plugin-vue": "^6.0.5",
    "vite": "^8.0.1"
  }
}
